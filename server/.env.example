# Express port
PORT=3000

# SQLite database file path (inside the container or local folder)
DB_PATH=./data/app.db

# environment: production or development
NODE_ENV=development

# only used when NODE_ENV != production to allow the Vite dev server
CORS_ORIGIN=http://localhost:5173